# 🇨🇳 电信领豆抢兑 V2.0 - 青龙面板配置清单

本清单包含该项目的脚本功能说明、青龙面板定时任务配置建议、环境变量设置以及依赖安装指南。

## 📁 脚本功能清单

| 脚本文件名 | 功能描述 | 建议 Cron 表达式 | 备注 |
| :--- | :--- | :--- | :--- |
| **`china_telecom_task.js`** | **日常签到与领豆**<br>执行每日签到、查询金豆余额、判断连签状态等日常任务。 | `30 7 * * *` | 建议每天早晨运行一次，防止漏签。 |
| **`china_telecom_exchange.py`** | **金豆抢兑话费**<br>利用金豆兑换话费奖励，支持整点抢兑（9点/12点/13点/23点等场次）。 | `59 8,11,12,22 * * *` | 需根据实际开放场次调整，建议在抢兑时间前1分钟运行。<br>脚本会自动等待至整点。 |

---

## 📥 拉库指令 (部署)

如果你已将代码上传至 GitHub 或 Gitee，推荐使用 `ql repo` 命令拉取，方便自动更新。

**拉库命令**:
```bash
ql repo https://github.com/BlackLK/dianxin.git "china_telecom_task|china_telecom_exchange" "" "sendNotify|china_telecom_encryption"
```

**参数说明**:
1. **仓库链接**: 本项目 GitHub 地址 `https://github.com/BlackLK/dianxin.git`。
2. **`"china_telecom_task|china_telecom_exchange"`**: 指定要创建任务的脚本文件（支持正则）。
3. **`""`**: 不设置黑名单，保持为空。
4. **`"sendNotify|china_telecom_encryption"`**: 指定需要复制的依赖文件（不会创建为定时任务，但会被脚本引用）。

---

## ⚙️ 环境变量设置

请在青龙面板的“配置文件”或“环境变量”中添加以下变量：

### 1. 账号配置 (必须)
**变量名**: `chinaTelecomAccount`

**值格式**: 
```text
手机号#服务密码
```

**多账号支持**:
使用换行符或 `&` 符号分割多个账号。

**示例**:
```text
13300000001#123456
13300000002#654321
```

> **注意**: 
> - 如果需要在Python脚本中使用WxPusher推送，格式支持扩展为 `手机号#服务密码#WxPusherUID` (仅Python脚本特别处理)。
> - 服务密码是电信账户的6位数字密码。

### 2. 其他配置 (可选)
- **推送通知**: 依赖 `sendNotify.js`，支持青龙面板通用的通知变量（如 `PUSH_KEY`, `BARK_PUSH` 等）。
- **Python脚本内置配置**: `china_telecom_exchange.py` 内部可修改 `appToken` (WxPusher AppToken) 用于特定推送。

---

## 📦 依赖安装

在使用脚本前，请确保青龙面板已安装以下依赖。

### Node.js 依赖
请在“依赖管理” -> “NodeJs” 中添加：
```text
xmldom
got
crypto-js
node-rsa
```

### Python 依赖
请在“依赖管理” -> “Python3” 中添加：
```text
requests
pycryptodome
PyExecJS
beautifulsoup4
certifi
```
*(注意: `crypto` 相关库在 Python 中通常安装 `pycryptodome`，脚本中调用为 `Crypto`)*

---

## 🚀 使用技巧

1. **同级目录依赖**: `china_telecom_exchange.py` 运行时需要读取同目录下的 `china_telecom_encryption.js` 文件，请确保它们在同一文件夹内。
2. **缓存文件**: 脚本运行后会生成 `chinaTelecom_cache.json` 用于存储 Token，减少频繁登录验证，请勿随意删除。
3. **抢兑策略**: Python 脚本内置了时间判断逻辑，会自动识别当前接近的场次（如上午11点运行则准备12点或13点的场次），并提前等待。

---

## ⚠️ 免责声明
本脚本仅供学习交流使用，请勿用于非法用途。请仔细核对服务密码，以免造成账户锁定。
